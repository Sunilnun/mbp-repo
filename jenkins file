pipeline{
    agent any
    stages{
        stage('when blcok with defaults'){
            when{
                expression {BRANCH_NAME ==~ /(docker|kubernetes)}
            }
            steps{
                echo 'deploying to kubernetes'
            }
        }
    }
}
